cd C:\HYPERLIQUID
. .\.venv\Scripts\Activate.ps1

git@github.com:yoyowasa/HYPERLIQUID.git
https://github.com/yoyowasa/HYPERLIQUID.git

python -m dotenv run -- python run_bot.py pfpl --symbols ETH-PERP --order_usd 15 --testnet --dry-run

GO=live python -m dotenv run -- python run_bot.py pfpl --symbols ETH-PERP --order_usd 15


echo "# HYPERLIQUID" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/yoyowasa/HYPERLIQUID.git
git push -u origin main

git remote add origin https://github.com/yoyowasa/HYPERLIQUID.git
git branch -M main
git push -u origin main

#############################
# ② 変更を全部まとめてコミット #
#############################

# ステージ：作業ツリーの全変更（追加・更新・削除）を一括
git add -A          # = git add --all

# コミット
git commit -m "chore: bulk update after lint & format"

# プッシュ
git push

強制上書き
git fetch origin && git reset --hard origin/main && git clean -fdx


code tunnel --name home-dev

gitから上書き
git fetch origin main
git reset --hard origin/main
検証に進む。origin/main と作業ツリーの差分がゼロか確認。
git --no-pager diff --name-status origin/main
# 直近コミットIDを比較して一致を確認する
git --no-pager log -1 --oneline
git --no-pager log -1 --oneline origin/main
