# HYPERLIQUID_BOT_ENV
# function: BOT開発/CIで共有する「安全既定」の環境変数の見本（このファイルはコミットOK/.envはコミット禁止）

# --- Safety defaults ------------------------------------------------------
DRY_RUN=true                         # function: 既定で実発注を無効化（falseにしない限り約定しない）
# LIVE=false                         # function: 人間向けの見やすいフラグ（任意）。コード側で未使用なら削除可。

# --- Database (再現容易性) -----------------------------------------------
DATABASE_URL=sqlite:///bot.db        # function: CI/一時環境と同じ軽量DBで再現性を担保

# --- Exchange settings (任意) --------------------------------------------
EXCHANGE=hyperliquid                 # function: 取引所ID（アダプタ側で参照する想定）
NETWORK=testnet                      # function: 安全のため既定は testnet 相当（本番は "mainnet" 等に変更）

# --- Credentials (絶対に .env のみに書く。ここは空の見本) ----------------
# HL_PRIVATE_KEY=                    # function: ライブ署名用の秘密鍵（live取引時のみ設定）。.envにだけ入れる
# HL_ACCOUNT_ADDRESS=                  # function: メインウォレットの公開アドレス(0x...)。SDKの account_address に渡す

# --- Risk & execution (任意の初期値) --------------------------------------
MAX_POSITION_USD=200                 # function: 建玉の上限（USD）。必要に応じて調整
MAX_DRAWDOWN_PCT=5                   # function: セッションの許容DD%。超えたら停止ロジック用
SLIPPAGE_BPS=10                      # function: 許容スリッページ(ベーシスポイント)。10 = 0.10%

# --- Logging --------------------------------------------------------------
LOG_LEVEL=INFO                       # function: ログ出力レベル（DEBUG/INFO/WARN/ERROR）
